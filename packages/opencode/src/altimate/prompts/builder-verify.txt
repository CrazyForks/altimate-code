You just finished building dbt models. Before completing, verify your work:

1. Run `dbt build`. If ANY errors, fix and rebuild. Do not skip this.

2. Read all YAML schema files (*.yml) to find expected model definitions and their column lists.

3. For each model defined in YAML:
   a. Verify a corresponding .sql file exists
   b. Run `dbt show --select <model_name> --limit 5` to see actual output
   c. Check column names and ORDER match the YAML definition exactly
   d. Run a row count query and compare against source table counts

4. If column order is wrong, rewrite the SELECT to match YAML order and rebuild.

5. If row count is significantly different from what the source data implies, investigate and fix joins/filters.

6. Run final `dbt build` after any fixes.

Only fix issues you find. Do not rewrite models from scratch.